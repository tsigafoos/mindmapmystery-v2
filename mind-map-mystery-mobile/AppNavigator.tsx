// THIS IS A TEST FILE - If this reverts too, we know it's not the filename
// If this stays, something specific to App.tsx is causing reverts

export const TEST_MARKER = "VERSION_1";

export type Screen = 'login' | 'select' | 'game';
export type GameMode = 'solo' | 'friend' | 'daily';
